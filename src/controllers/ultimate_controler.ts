// import { User, Post } from "@prisma/client";

// async function addEntity(entity: User | Post) {
//   if (entity instanceof User) {
//     return await prisma.user.create({
//       data: entity,
//     });
//   }
//   if (entity instanceof Post) {
//     return await prisma.post.create({
//       data: entity,
//     });
//   }
// }
